# Next-agent geometry experiment prompt

Use this with a DCCLoop snapshot and an owner-approved run record. Without that record, prepare intake/experiment files only and request the missing run settings/limits together. Do not start native or paid work by interpreting this template as approval.

---

You are commissioned to test and improve **the geometry-modeling process** for a resumable reference-to-3D loop. The objective is actual faithful, editable and scene-efficient geometry on a bounded benchmark, not another broad architecture exercise or attractive materials.

Read, in order:

1. `docs/experiments/geometry-v1/COMMISSION.md`
2. `docs/experiments/geometry-v1/SELECTED_PRODUCTS.md` and `GEOMETRY_RUBRIC.md`
3. `docs/experiments/geometry-v1/EXPERIMENT_PLAN.json` and `TRIAL_MATRIX.csv`
4. `docs/experiments/geometry-v1/LOGGING_CONTRACT.md`, `LEARNING_LOG_TEMPLATE.md` and `EVENT_LEDGER_TEMPLATE.csv`.
5. The completed owner approval record, fixture/reference packets and applicable repository instructions.
6. `docs/design.md`, `docs/testing.md`, and only the runtime source needed for the experiment.

Use the owner-selected **Joriel Vase (D01)** and **Ziven Bowl in Blue (D02)**. These are the only commercial products in this campaign. Freeze the linked reference images and sourced dimensions, recording incomplete views and assumptions. Do not obtain a finished product mesh from the site’s 3D viewer. H01/H02 are separate synthetic validation geometries.

First establish exactly which phases, inputs, native operations, model/provider calls and budgets are authorized. Do not select a real product yourself. Do not refine an existing model or use a supplied finished mesh as builder input unless that specific experimental use is authorized. Keep originals and earlier evidence read-only.

Complete capability/intake checks and freeze the evaluator before creative trials. The public adapter is finite and partly product-specific; identify the minimum missing experiment capability rather than pretending a generic runner already exists. Make only the isolated harness/recipe changes required by the approved experiment. Preserve cancellation, ownership, receipt, budget and freshness guards; no production-controller redesign or installation without separate authority.

Then perform the authorized phases:

- **E0:** demonstrate independent positive/negative measurement controls and freeze asset-specific tolerances, cameras, views and scene probes.
- **E1:** compare direct construction with explicit anatomy/measurement-scaffold construction on the same development cases and budgets.
- **E2:** fork identical initial geometry into equal-budget self-review and separate-reviewer feedback branches, one correction each. Use numbered visual callouts and a localized geometric diagnosis for the structured feedback.
- **E3:** test one preregistered optimization policy while protecting fidelity/editability; retain its raw parent even if the optimized variant fails.
- **E4:** lock the complete process and compare baseline versus challenger on the two sealed unseen synthetic geometries. Allow only the prescribed within-trial correction; do not tune the process from earlier holdout scores.

Keep the coordinator, builder and independent reviewer structure. Charge all roles and failed/discarded work to the ledger. Use neutral/clay/wire/grazing-light proof views with pinned settings; no material, texture, normal-map, displacement or camera trick may substitute for geometry.

Maintain the work/revision/learning log during execution, not from memory at the end. Explain what worked, what failed and why, distinguishing observed evidence from causal hypotheses. Retain failed/discarded attempts. Reconcile token and time sources once; provide product/phase/trial/revision/role breakdowns, a shared-work bucket and campaign totals. Keep measured, estimated and unavailable values separate.

For every attempt, retain candidate/parent hashes, source recipe, native readback, view identities, before/after annotated comparisons, criterion verdicts, reviewer provenance, timing, actual/estimated usage and exact failure/stop reasons. Keep independent final evaluation separate from advisory feedback. Never infer human approval from a model review or passing technical checks.

Continue ordinary implementation and testing inside the approved phases without requesting approval for each command. Stop for missing mandatory inputs, unauthorized scope, violated guards, compromised evaluator separation or a binding budget. An ordinary visual failure is an experimental result; finish its bounded trace and preserve it. Do not erase trials, reset counters or repeat an exposed holdout as if unseen.

Keep acceptance of the two real development products separate from synthetic transfer. Do not claim unseen-real-product generalization. The proposed matrix contains 26 trials and at most 34 creative passes; authorize stages and limits before running them.

Return the exact benchmark outcomes, paired comparisons, limitations, and a reproducible recommended geometry workflow. Include every failure, skipped/missing trial, actual native versus simulated work, total cost/time by role and trial, and the best eligible native assets with their real approval status. If quality remains inadequate, state the unresolved cause and propose one next bounded hypothesis; do not claim success or continue indefinitely.

No material-library work, production rendering, Max/other-DCC implementation, public asset upload, or new campaign is included. The documentation's numerical caps and thresholds are proposals until adopted in the owner's frozen run record.

Use `[obs]` for operations actually executed, `[repo path:line]` for source claims, `[docs]` with official sources for external capabilities, and `[ESTIMATE]` for inference or proposed values. Source inspection, a simulation and a real native run are different evidence categories.
