# Geometry campaign planning record

Planning status as of 2026-09-16 Pacific time. This records this documentation task; it is not a modeling-trial ledger. [obs]

| Revision | Work completed | Decision and rationale | What worked / limitation |
|---|---|---|---|
| P01 | Drafted construction, feedback, optimization and validation comparisons; rubric and blank results | Separate geometry quality, editability, scene use and human approval; preserve failed trials | Same-parent, equal-budget feedback comparison avoids attributing extra revision opportunity to reviewer quality; this is a design rationale, not an experimental result. [ESTIMATE] |
| P02 | Recorded owner selection of Joriel Vase and Ziven Bowl; inspected product pages, dimension records and displayed images | Use both as development products; remove the draft third-product slot; keep two synthetic transfer fixtures | Product Details and public dimension data agreed. Gallery evidence remained incomplete for hidden surfaces. No claim of unseen-commercial-product transfer. [obs] |
| P03 | Updated matrix from the unexecuted 30-row/42-pass draft to 26 rows/34 maximum passes | Exactly two commercial products were selected. Removed four two-pass validation traces for the unselected third product: 30 − 4 = 26; 42 − 8 = 34 | This is a preregistration edit before execution, not deletion of failed results or a change to historical controller test oracles. [obs] |
| P04 | Added required event, revision, token/time and learning-log deliverables | Owner requested all work/revisions, what worked/failed/why and consumption for future refinement | Preserves raw usage sources, failed attempts, uncertainty and shared work; logging behavior is specified, not implemented or runtime-tested here. [obs] |

The two-product documentation received a separate read-only consistency review. Its key correction was to require human approval for the selected **development** deliverables after removing the third real validation product; nonexistent real-product P1 validation candidates must not remain in the endpoint. [obs]

The staged whitespace check initially flagged CSV CRLF line endings. They were normalized to LF; parsed CSV cells were checked unchanged. This was a packaging-format correction, with no trial/result change. [obs]

## Evidence and consumption boundaries

This task inspected repository documentation/source, public product pages/dimension data and three displayed images; wrote the commission and logging templates; and performed the static checks recorded in PLANNING_VALIDATION.json. It did not execute Blender or any modeling experiment. The two-product trial matrix remains unstarted. [obs]

Current planning task token counts and active-time totals were not collected at revision granularity. They are **unavailable**, not zero, and are not assigned to the future Joriel/Ziven modeling trials. Do not reconstruct measured-looking numbers from text length or task timestamps. A later saved-log audit may establish aggregate usage with explicit allocation limitations. [obs] [ESTIMATE]

Unresolved before execution: reference image bytes/hashes; acceptable unseen-surface approximations; intended viewing scale and tolerances; scene/performance targets; model/execution settings; finite token/monetary limits; authorized phase scope; evaluator custody. The two real products themselves are selected. [obs]
